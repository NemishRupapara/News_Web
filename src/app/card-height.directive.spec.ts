import { CardHeightDirective } from './card-height.directive';

describe('CardHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHeightDirective();
    expect(directive).toBeTruthy();
  });
});
